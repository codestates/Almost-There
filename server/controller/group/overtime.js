const { _groups } = require('../../models');
const { users_groups } = require('../../models');
module.exports = {
  post: async(req, res) => {
    // const tasks = await users_groups.findAll({ include: _groups });
    //   console.log(tasks)
  }
};
